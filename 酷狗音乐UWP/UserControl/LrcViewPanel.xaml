<UserControl
    x:Class="KuGouMusicUWP.UserControlClass.LrcViewPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KuGouMusicUWP.UserControlClass"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="unlrcview">
            <Setter Property="Margin" Value="3"></Setter>
            <Setter Property="Foreground" Value="{ThemeResource KuGou-Front2}"></Setter>
            <Setter Property="FontSize" Value="14"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        </Style>
        <Style TargetType="TextBlock" x:Key="islrcview">
            <Setter Property="Margin" Value="3"></Setter>
            <Setter Property="Foreground" Value="{ThemeResource KuGou-Foreground}"></Setter>
            <Setter Property="FontSize" Value="14"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel x:Name="lrcdatapanel" HorizontalAlignment="Center"></StackPanel>
        <TextBlock x:Name="nolrcview" Visibility="Collapsed" Style="{StaticResource islrcview}" VerticalAlignment="Center">暂无歌词</TextBlock>
    </Grid>
</UserControl>
