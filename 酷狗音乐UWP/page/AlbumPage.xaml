<Page
    x:Class="酷狗音乐UWP.page.AlbumPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:酷狗音乐UWP.page"
    xmlns:usercon="using:酷狗音乐UWP.UserControlClass"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FF37ADDD">
        <Grid x:Name="albuminfo_Grid" Height="170" VerticalAlignment="Top">
            <Grid.Background>
                <ImageBrush Stretch="UniformToFill" ImageSource="{Binding imgurl}"/>
            </Grid.Background>
            <Grid Height="50" VerticalAlignment="Top">
                <Button Click="BackBtn_Clicked" Width="50" Height="50" Background="{x:Null}">
                    <SymbolIcon Foreground="White" Symbol="Back"></SymbolIcon>
                </Button>
                <TextBlock Margin="55,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" FontSize="18" Text="{Binding albumname}">专辑</TextBlock>
                <Button  Width="50" Height="50" Background="{x:Null}" HorizontalAlignment="Right">
                    <SymbolIcon Foreground="White" Symbol="More"></SymbolIcon>
                </Button>
            </Grid>
            <Grid Margin="0,50,0,0" Height="120" VerticalAlignment="Top">
                <Button Background="{x:Null}" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" Height="40">
                        <Ellipse Width="40" Height="40">
                            <Ellipse.Fill>
                                <ImageBrush x:Name="singerimg" Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <StackPanel Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <TextBlock Foreground="White" FontSize="13" Text="{Binding singername}">童可可</TextBlock>
                            <TextBlock Foreground="White" FontSize="12" Text="{Binding publishtime}">2016-05-13 发行</TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
        <Grid Margin="0,170,0,0" HorizontalAlignment="Right" VerticalAlignment="Top" Height="25" Width="150">
            <FontIcon Width="20" Height="20" FontFamily="Segoe MDL2 Assets" Glyph="&#xE713;" Foreground="#FFE7F4FB" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6,3,0,2" />
            <TextBlock Margin="-12,3,14,2" HorizontalAlignment="Right" Foreground="#FFE7F4FB" VerticalAlignment="Center">
                <Run Text="对多首歌曲操作"/>
            </TextBlock>
        </Grid>
        <ScrollViewer Margin="0,195,0,63">
            <ListView x:Name="SongList"  Background="{x:Null}" CanReorderItems="True" SelectionMode="Single">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="BorderBrush" Value="#7F808080"/>
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Visibility="Visible" Margin="0,0,0,2" Background="#BFFFFFFF" Height="60" BorderThickness="0,0,0,2" VerticalAlignment="Top">
                            <StackPanel Margin="15,5,0,5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontSize="16">
                        							<Run Text="{Binding title}"/>
                                    </TextBlock>
                                    <Image Margin="10,0,0,0" Height="13" Width="20" Visibility="{Binding hasmv}" Source="ms-appx:///Assets/image/mvicon.png" Stretch="Fill"></Image>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontSize="14" Foreground="#FF878787">
                        								<Run Text="{Binding singername}"/>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                            <AppBarButton Width="50" Height="50" Icon="More" Background="{x:Null}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#FF43BFEF"/>
                            <AppBarButton Margin="0,0,50,0" Width="50" Height="50" Icon="Add" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#FF44BEEE"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <usercon:PlayerSmall VerticalAlignment="Bottom"></usercon:PlayerSmall>
    </Grid>
</Page>
